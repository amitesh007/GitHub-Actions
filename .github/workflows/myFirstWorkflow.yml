name: my-first-workflow
on: 
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
  push:

jobs:
  job1:
    name: JOB1
    runs-on: ubuntu-latest
    steps:
      - name: Step 1
        run: |
          pwd
          ls -ltr
          hostname
     
